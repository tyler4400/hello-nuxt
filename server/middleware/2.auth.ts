export default defineEventHandler(async (event) => {
  // 假数据，模拟从 token / session 取到的用户
  // const user = await Promise.resolve({ isLogin: false, userName: 'jackson' })
  // console.log(user)
  //
  // if (!user.isLogin) {
  //   // 未登录：抛出自定义错误
  //   throw createError({
  //     statusCode: 401,
  //     statusMessage: 'unauthorized user!!!',
  //   })
  // }
})
